Webapp
======

1. Compile Scala into JavaScript:
```sh
$ sbt ~fastLinkJS
```

2. Download JS dependencies:
```sh
$ npm install
```

3. Run dev server
```sh
$ npm start
```

4. Open http://localhost:8080. Changes will refresh page automatically.
